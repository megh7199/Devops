import React from 'react';
import '../App.css';

const Home = () => {
  return (
    <div>
      <p>
        This is a simple example of using React to Query the ticketmaster API. It provides a list of upcoming events, different venues and attractions. You can get different details about all these all in one place.
      </p>

    </div>
  );
};

export default Home;
